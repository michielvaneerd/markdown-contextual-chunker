{
  "name": "@michielvaneerd/markdown-contextual-chunker",
  "version": "1.0.0",
  "description": "A Markdown chunker that adds the header hierarchy to each chunk and converts tables to lists so that they can easily be split over several chunks if needed.",
  "main": "./src/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/michielvaneerd/markdown-contextual-chunker.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "markdown-contextual-chunker": "./bin/markdown-contextual-chunker"
  },
  "keywords": [],
  "type": "module",
  "devDependencies": {
    "js-tiktoken": "^1.0.21"
  },
  "dependencies": {
    "marked": "^18.0.13"
  }
}